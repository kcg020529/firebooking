# Antigravity handoff

## 현재 상태

- 작업 브랜치: `feat/interactive-course-map`
- 목표: 필드 골프장 상세 화면에서 2D 코스맵의 제품 느낌을 확인하는 샘플
- 구현과 검증을 완료했으며 원격 브랜치에서 Preview 배포할 수 있는 상태다.
- DB/API 변경 없이 프론트엔드 데모 데이터만 사용한다.

## 먼저 확인할 것

1. 앱을 실행하고 메인 화면에서 **필드** 골프장 하나를 선택한다.
2. 골프장 정보 아래의 `2D 코스맵`을 데스크톱과 모바일 폭에서 확인한다.
3. 지도 위 홀 또는 오른쪽 숫자 버튼을 눌러 선택 정보가 바뀌는지 확인한다.
4. `전체`, `OUT 1–9`, `IN 10–18` 전환을 확인한다.
5. 스크린 골프장 상세에는 코스맵이 나타나지 않는지 확인한다.

## 다음 결정 사항

- 이 시각 방향을 유지할지 검토한다.
- 실제 기능으로 발전시키면 하드코딩한 `HOLES`를 골프장별 DB/API 데이터로 분리한다.
- 실제 골프장 배치도·거리·공략 데이터의 출처와 이미지/지도 라이선스를 확정한다.
- 실제 지형을 반영할 경우 골프장별 SVG path 데이터 또는 관리자용 편집 도구 범위를 정한다.

## 주요 파일

- `components/course-map/CourseMap.js`: 18홀 데모 데이터, SVG 지도, 구간/홀 선택 UI
- `app/courses/[id]/page.js`: 필드 타입에만 코스맵을 노출하는 연결부

## 검증 결과

- `npm run lint`: 통과
- `npm test`: 118 passed, 1 skipped, 0 failed
- `npm run build`: 통과
- 참고: 최초 샌드박스 실행에서는 Node 테스트 러너의 자식 프로세스 생성이 `EPERM`으로 차단됐고, 승인된 실행에서 전체 통과했다.

## 완료 항목

- [x] 새 브랜치 `feat/interactive-course-map` 생성
- [x] 반응형 SVG 18홀 코스 배치도 샘플 구현
- [x] 선택 홀 강조 및 PAR·거리·핸디캡·공략 팁 표시
- [x] 전체/OUT/IN 구간 필터 구현
- [x] 지도와 숫자 버튼의 키보드/접근성 레이블 적용
- [x] 필드 상세 페이지에만 코스맵 연결
- [x] 린트, 전체 테스트, 프로덕션 빌드 검증
- [x] Antigravity 인수인계 문서 작성
